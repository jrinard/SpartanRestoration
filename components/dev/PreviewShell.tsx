"use client";

import type { ReactNode } from "react";

/** Vision pack stub — Forge shell removed in Phase 5. */
export function PreviewShell({ children }: { children?: ReactNode } & Record<string, unknown>) {
  return children ?? null;
}
