"use client";

import type { IconFramePreviewSettings } from "@/lib/icon-frame-preview";

/** Vision pack stub — Forge editor removed in Phase 5. */
export function IconFramePreviewControls(_props: {
  settings: IconFramePreviewSettings;
  onChange: (patch: Partial<IconFramePreviewSettings>) => void;
  ariaPrefix: string;
}): null {
  return null;
}
