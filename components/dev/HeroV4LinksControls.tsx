"use client";

import type { NavBarLink } from "@/lib/nav-bar-preview";

/** Vision pack stub — Forge editor removed in Phase 5. */
export function HeroV4LinksControls(_props: {
  title: string;
  links: NavBarLink[];
  onChange: (links: NavBarLink[]) => void;
  allowCurrentPage?: boolean;
  addLabel?: string;
}): null {
  return null;
}
