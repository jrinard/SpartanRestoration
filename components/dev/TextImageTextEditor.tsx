"use client";

/** Vision pack stub — Forge editor removed in Phase 5. */
export function TextImageTextEditor(_props: {
  value: string;
  onChange: (value: string) => void;
  onClose: () => void;
  dialogTitle: string;
  dialogAriaLabel: string;
  inputAriaLabel: string;
  multiline?: boolean;
  rows?: number;
  className?: string;
}): null {
  return null;
}
