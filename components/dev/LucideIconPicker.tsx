"use client";

import type { SiteIconName } from "@/lib/site-icons";

/** Vision pack stub — Forge editor removed in Phase 5. */
export function LucideIconPicker(_props: {
  value: SiteIconName;
  onChange: (iconName: SiteIconName) => void;
  onClose: () => void;
  className?: string;
}): null {
  return null;
}
