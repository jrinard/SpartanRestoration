"use client";

import type { TopBarSocialLink } from "@/lib/top-bar-social";

/** Vision pack stub — Forge editor removed in Phase 5. */
export function TopBarSocialLinksControls(_props: {
  links: TopBarSocialLink[];
  onChange: (links: TopBarSocialLink[]) => void;
  className?: string;
}): null {
  return null;
}
